Podiums = podiums))
invisible(tottable <- left_join(tottable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums,
Wins = wins))
dtable <- bind_cols(wintable, podtable)
rm(wintable, podtable)
# # Static table
# dtable |>
#   gt() |>
#   tab_header(
#     title = md("## Top ten F1 drivers")
#   ) |>
#   gt_img_rows(columns = Nationality, height = 15) |>
#   gt_img_rows(columns = nationality, height = 15) |>
#   cols_align(
#     align = "center",
#     columns = 2:3) |>
#   cols_align(
#     align = 'center',
#     columns = 4:5
#   ) |>
#   tab_spanner(label = 'Most wins',
#               columns = 2:3) |>
#   tab_spanner(label = 'Most podiums',
#               columns = 5:6) |>
#   gt_theme_538()
# Responsive table
tottable |>
gt(rowname_col = 'Name') |>
tab_header(title = html('<h2>Top F1 drivers</h2>'),
) |>
fmt_image(Nationality, height = 15) |>
cols_align(align = 'center', columns = 2:4) |>
opt_interactive(use_pagination = T,
use_sorting = T) |>
tab_options(table.background.color = 'white',
table.font.style = 'Roboto',
table.border.top.color = 'white',
heading.align = 'center',
row.striping.include_table_body =FALSE,
heading.border.bottom.color = "white",
row_group.border.bottom.color = "white",
row_group.border.top.color = "white"
)
#| label: Table
#| code-summary: 'Table design'
# Building df
wintable <-  drivers |>
select(name, nationality, wins, podiums) |>
filter(!is.na(wins)) |>
arrange(desc(wins)) |>
slice(1:20)
podtable <-  drivers |>
select(name, nationality, podiums, wins) |>
filter(!is.na(podiums)) |>
arrange(desc(podiums)) |>
slice(1:20)
wintable$nationality <- c('GB', 'DE', 'NL', 'DE', 'FR', 'BR', 'ES', 'GB', 'GB', 'AT', 'GB', 'AR', 'DE', 'BR', 'GB', 'FI', 'FI', 'GB', 'GB', 'BR')
podtable$nationality <- c('GB', 'DE', 'DE', 'NL', 'ES', 'FR', 'FI', 'BR', 'BR', 'FI', 'GB', 'BR', 'GB', 'DE', 'AT', 'FI', 'GB', 'AT', 'AR', 'GB')
invisible(tottable <- full_join(wintable, podtable))
# Country flag images
flags <- flags |>
select(URL, `Alpha-2 code`)
## merge
invisible(wintable <- left_join(wintable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins) |>
rename(nationality = URL))
invisible(podtable <- left_join(podtable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums))
invisible(tottable <- left_join(tottable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums,
Wins = wins))
dtable <- bind_cols(wintable, podtable)
rm(wintable, podtable)
# # Static table
# dtable |>
#   gt() |>
#   tab_header(
#     title = md("## Top ten F1 drivers")
#   ) |>
#   gt_img_rows(columns = Nationality, height = 15) |>
#   gt_img_rows(columns = nationality, height = 15) |>
#   cols_align(
#     align = "center",
#     columns = 2:3) |>
#   cols_align(
#     align = 'center',
#     columns = 4:5
#   ) |>
#   tab_spanner(label = 'Most wins',
#               columns = 2:3) |>
#   tab_spanner(label = 'Most podiums',
#               columns = 5:6) |>
#   gt_theme_538()
# Responsive table
tottable |>
gt(rowname_col = 'Name') |>
tab_header(title = html('<h2>Top F1 drivers</h2>'),
) |>
fmt_image(Nationality, height = 15) |>
cols_align(align = 'center', columns = 2:4) |>
opt_interactive(use_pagination = T,
use_sorting = T) |>
tab_options(table.background.color = 'white',
table.font.style = 'Roboto',
table.border.top.color = 'white',
heading.align = 'center',
row.striping.include_table_body =FALSE,
heading.border.bottom.color = "white",
row_group.border.bottom.color = "white",
row_group.border.top.color = "white"
)
#| label: Table
#| code-summary: 'Table design'
# Building df
wintable <-  drivers |>
select(name, nationality, wins, podiums) |>
filter(!is.na(wins)) |>
arrange(desc(wins)) |>
slice(1:20)
podtable <-  drivers |>
select(name, nationality, podiums, wins) |>
filter(!is.na(podiums)) |>
arrange(desc(podiums)) |>
slice(1:20)
wintable$nationality <- c('GB', 'DE', 'NL', 'DE', 'FR', 'BR', 'ES', 'GB', 'GB', 'AT', 'GB', 'AR', 'DE', 'BR', 'GB', 'FI', 'FI', 'GB', 'GB', 'BR')
podtable$nationality <- c('GB', 'DE', 'DE', 'NL', 'ES', 'FR', 'FI', 'BR', 'BR', 'FI', 'GB', 'BR', 'GB', 'DE', 'AT', 'FI', 'GB', 'AT', 'AR', 'GB')
invisible(tottable <- full_join(wintable, podtable))
# Country flag images
flags <- flags |>
select(URL, `Alpha-2 code`)
## merge
invisible(wintable <- left_join(wintable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins) |>
rename(nationality = URL))
invisible(podtable <- left_join(podtable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums))
invisible(tottable <- left_join(tottable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums,
Wins = wins))
dtable <- bind_cols(wintable, podtable)
rm(wintable, podtable)
# # Static table
# dtable |>
#   gt() |>
#   tab_header(
#     title = md("## Top ten F1 drivers")
#   ) |>
#   gt_img_rows(columns = Nationality, height = 15) |>
#   gt_img_rows(columns = nationality, height = 15) |>
#   cols_align(
#     align = "center",
#     columns = 2:3) |>
#   cols_align(
#     align = 'center',
#     columns = 4:5
#   ) |>
#   tab_spanner(label = 'Most wins',
#               columns = 2:3) |>
#   tab_spanner(label = 'Most podiums',
#               columns = 5:6) |>
#   gt_theme_538()
# Responsive table
tottable |>
gt(rowname_col = 'Name') |>
tab_header(title = html('<h2>Top F1 drivers</h2>'),
) |>
fmt_image(Nationality, height = 15) |>
cols_align(align = 'center', columns = 2:4) |>
opt_interactive(use_pagination = T,
use_sorting = T) |>
tab_options(table.background.color = 'white',
table.font.style = 'Roboto',
table.border.top.color = 'white',
heading.align = 'center',
row.striping.include_table_body =FALSE,
heading.border.bottom.color = "white",
row_group.border.bottom.color = "white",
row_group.border.top.color = "white"
)
dtable <- bind_cols(wintable, podtable)
# Building df
wintable <-  drivers |>
select(name, nationality, wins, podiums) |>
filter(!is.na(wins)) |>
arrange(desc(wins)) |>
slice(1:20)
podtable <-  drivers |>
select(name, nationality, podiums, wins) |>
filter(!is.na(podiums)) |>
arrange(desc(podiums)) |>
slice(1:20)
wintable$nationality <- c('GB', 'DE', 'NL', 'DE', 'FR', 'BR', 'ES', 'GB', 'GB', 'AT', 'GB', 'AR', 'DE', 'BR', 'GB', 'FI', 'FI', 'GB', 'GB', 'BR')
podtable$nationality <- c('GB', 'DE', 'DE', 'NL', 'ES', 'FR', 'FI', 'BR', 'BR', 'FI', 'GB', 'BR', 'GB', 'DE', 'AT', 'FI', 'GB', 'AT', 'AR', 'GB')
invisible(tottable <- full_join(wintable, podtable))
#| label: Table
#| code-summary: 'Table design'
#| results: hide
#| fig-keep: 'all'
# Building df
wintable <-  drivers |>
select(name, nationality, wins, podiums) |>
filter(!is.na(wins)) |>
arrange(desc(wins)) |>
slice(1:20)
podtable <-  drivers |>
select(name, nationality, podiums, wins) |>
filter(!is.na(podiums)) |>
arrange(desc(podiums)) |>
slice(1:20)
wintable$nationality <- c('GB', 'DE', 'NL', 'DE', 'FR', 'BR', 'ES', 'GB', 'GB', 'AT', 'GB', 'AR', 'DE', 'BR', 'GB', 'FI', 'FI', 'GB', 'GB', 'BR')
podtable$nationality <- c('GB', 'DE', 'DE', 'NL', 'ES', 'FR', 'FI', 'BR', 'BR', 'FI', 'GB', 'BR', 'GB', 'DE', 'AT', 'FI', 'GB', 'AT', 'AR', 'GB')
tottable <- full_join(wintable, podtable)
# Country flag images
flags <- flags |>
select(URL, `Alpha-2 code`)
## merge
wintable <- left_join(wintable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins) |>
rename(nationality = URL)
podtable <- left_join(podtable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums)
tottable <- left_join(tottable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums,
Wins = wins)
dtable <- bind_cols(wintable, podtable)
rm(wintable, podtable)
# # Static table
# dtable |>
#   gt() |>
#   tab_header(
#     title = md("## Top ten F1 drivers")
#   ) |>
#   gt_img_rows(columns = Nationality, height = 15) |>
#   gt_img_rows(columns = nationality, height = 15) |>
#   cols_align(
#     align = "center",
#     columns = 2:3) |>
#   cols_align(
#     align = 'center',
#     columns = 4:5
#   ) |>
#   tab_spanner(label = 'Most wins',
#               columns = 2:3) |>
#   tab_spanner(label = 'Most podiums',
#               columns = 5:6) |>
#   gt_theme_538()
# Responsive table
tottable |>
gt(rowname_col = 'Name') |>
tab_header(title = html('<h2>Top F1 drivers</h2>'),
) |>
fmt_image(Nationality, height = 15) |>
cols_align(align = 'center', columns = 2:4) |>
opt_interactive(use_pagination = T,
use_sorting = T) |>
tab_options(table.background.color = 'white',
table.font.style = 'Roboto',
table.border.top.color = 'white',
heading.align = 'center',
row.striping.include_table_body =FALSE,
heading.border.bottom.color = "white",
row_group.border.bottom.color = "white",
row_group.border.top.color = "white"
)
#| label: Table
#| code-summary: 'Table design'
#| results: hide
#| fig-keep: 'all'
# Building df
wintable <-  drivers |>
select(name, nationality, wins, podiums) |>
filter(!is.na(wins)) |>
arrange(desc(wins)) |>
slice(1:20)
podtable <-  drivers |>
select(name, nationality, podiums, wins) |>
filter(!is.na(podiums)) |>
arrange(desc(podiums)) |>
slice(1:20)
wintable$nationality <- c('GB', 'DE', 'NL', 'DE', 'FR', 'BR', 'ES', 'GB', 'GB', 'AT', 'GB', 'AR', 'DE', 'BR', 'GB', 'FI', 'FI', 'GB', 'GB', 'BR')
podtable$nationality <- c('GB', 'DE', 'DE', 'NL', 'ES', 'FR', 'FI', 'BR', 'BR', 'FI', 'GB', 'BR', 'GB', 'DE', 'AT', 'FI', 'GB', 'AT', 'AR', 'GB')
tottable <- full_join(wintable, podtable)
# Country flag images
flags <- flags |>
select(URL, `Alpha-2 code`)
## merge
wintable <- left_join(wintable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins) |>
rename(nationality = URL)
podtable <- left_join(podtable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums)
tottable <- left_join(tottable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums,
Wins = wins)
dtable <- bind_cols(wintable, podtable)
rm(wintable, podtable)
# # Static table
# dtable |>
#   gt() |>
#   tab_header(
#     title = md("## Top ten F1 drivers")
#   ) |>
#   gt_img_rows(columns = Nationality, height = 15) |>
#   gt_img_rows(columns = nationality, height = 15) |>
#   cols_align(
#     align = "center",
#     columns = 2:3) |>
#   cols_align(
#     align = 'center',
#     columns = 4:5
#   ) |>
#   tab_spanner(label = 'Most wins',
#               columns = 2:3) |>
#   tab_spanner(label = 'Most podiums',
#               columns = 5:6) |>
#   gt_theme_538()
# Responsive table
tottable |>
gt(rowname_col = 'Name') |>
tab_header(title = html('<h2>Top F1 drivers</h2>'),
) |>
fmt_image(Nationality, height = 15) |>
cols_align(align = 'center', columns = 2:4) |>
opt_interactive(use_pagination = T,
use_sorting = T) |>
tab_options(table.background.color = 'white',
table.font.style = 'Roboto',
table.border.top.color = 'white',
heading.align = 'center',
row.striping.include_table_body =FALSE,
heading.border.bottom.color = "white",
row_group.border.bottom.color = "white",
row_group.border.top.color = "white"
)
#| label: Table
#| code-summary: 'Table design'
#| message: false
# Building df
wintable <-  drivers |>
select(name, nationality, wins, podiums) |>
filter(!is.na(wins)) |>
arrange(desc(wins)) |>
slice(1:20)
podtable <-  drivers |>
select(name, nationality, podiums, wins) |>
filter(!is.na(podiums)) |>
arrange(desc(podiums)) |>
slice(1:20)
wintable$nationality <- c('GB', 'DE', 'NL', 'DE', 'FR', 'BR', 'ES', 'GB', 'GB', 'AT', 'GB', 'AR', 'DE', 'BR', 'GB', 'FI', 'FI', 'GB', 'GB', 'BR')
podtable$nationality <- c('GB', 'DE', 'DE', 'NL', 'ES', 'FR', 'FI', 'BR', 'BR', 'FI', 'GB', 'BR', 'GB', 'DE', 'AT', 'FI', 'GB', 'AT', 'AR', 'GB')
tottable <- full_join(wintable, podtable)
# Country flag images
flags <- flags |>
select(URL, `Alpha-2 code`)
## merge
wintable <- left_join(wintable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins) |>
rename(nationality = URL)
podtable <- left_join(podtable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums)
tottable <- left_join(tottable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums,
Wins = wins)
dtable <- bind_cols(wintable, podtable)
rm(wintable, podtable)
# # Static table
# dtable |>
#   gt() |>
#   tab_header(
#     title = md("## Top ten F1 drivers")
#   ) |>
#   gt_img_rows(columns = Nationality, height = 15) |>
#   gt_img_rows(columns = nationality, height = 15) |>
#   cols_align(
#     align = "center",
#     columns = 2:3) |>
#   cols_align(
#     align = 'center',
#     columns = 4:5
#   ) |>
#   tab_spanner(label = 'Most wins',
#               columns = 2:3) |>
#   tab_spanner(label = 'Most podiums',
#               columns = 5:6) |>
#   gt_theme_538()
# Responsive table
tottable |>
gt(rowname_col = 'Name') |>
tab_header(title = html('<h2>Top F1 drivers</h2>'),
) |>
fmt_image(Nationality, height = 15) |>
cols_align(align = 'center', columns = 2:4) |>
opt_interactive(use_pagination = T,
use_sorting = T) |>
tab_options(table.background.color = 'white',
table.font.style = 'Roboto',
table.border.top.color = 'white',
heading.align = 'center',
row.striping.include_table_body =FALSE,
heading.border.bottom.color = "white",
row_group.border.bottom.color = "white",
row_group.border.top.color = "white"
)
#| label: Table
#| code-summary: 'Table design'
#| message: false
# Building df
wintable <-  drivers |>
select(name, nationality, wins, podiums) |>
filter(!is.na(wins)) |>
arrange(desc(wins)) |>
slice(1:20)
podtable <-  drivers |>
select(name, nationality, podiums, wins) |>
filter(!is.na(podiums)) |>
arrange(desc(podiums)) |>
slice(1:20)
wintable$nationality <- c('GB', 'DE', 'NL', 'DE', 'FR', 'BR', 'ES', 'GB', 'GB', 'AT', 'GB', 'AR', 'DE', 'BR', 'GB', 'FI', 'FI', 'GB', 'GB', 'BR')
podtable$nationality <- c('GB', 'DE', 'DE', 'NL', 'ES', 'FR', 'FI', 'BR', 'BR', 'FI', 'GB', 'BR', 'GB', 'DE', 'AT', 'FI', 'GB', 'AT', 'AR', 'GB')
tottable <- full_join(wintable, podtable)
# Country flag images
flags <- flags |>
select(URL, `Alpha-2 code`)
## merge
wintable <- left_join(wintable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins) |>
rename(nationality = URL)
podtable <- left_join(podtable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums)
tottable <- left_join(tottable, flags, by = join_by('nationality' == `Alpha-2 code`)) |>
select(name, URL, wins, podiums) |>
rename(Name = name,
Nationality = URL,
Podiums = podiums,
Wins = wins)
dtable <- bind_cols(wintable, podtable)
rm(wintable, podtable)
# # Static table
# dtable |>
#   gt() |>
#   tab_header(
#     title = md("## Top ten F1 drivers")
#   ) |>
#   gt_img_rows(columns = Nationality, height = 15) |>
#   gt_img_rows(columns = nationality, height = 15) |>
#   cols_align(
#     align = "center",
#     columns = 2:3) |>
#   cols_align(
#     align = 'center',
#     columns = 4:5
#   ) |>
#   tab_spanner(label = 'Most wins',
#               columns = 2:3) |>
#   tab_spanner(label = 'Most podiums',
#               columns = 5:6) |>
#   gt_theme_538()
# Responsive table
tottable |>
gt(rowname_col = 'Name') |>
tab_header(title = html('<h2>Top F1 drivers</h2>'),
) |>
fmt_image(Nationality, height = 15) |>
cols_align(align = 'center', columns = 2:4) |>
opt_interactive(use_pagination = T,
use_sorting = T) |>
tab_options(table.background.color = 'white',
table.font.style = 'Roboto',
table.border.top.color = 'white',
heading.align = 'center',
row.striping.include_table_body =FALSE,
heading.border.bottom.color = "white",
row_group.border.bottom.color = "white",
row_group.border.top.color = "white"
)
